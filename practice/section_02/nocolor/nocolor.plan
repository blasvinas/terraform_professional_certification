aws_security_group.test_sg: Refreshing state... [id=sg-071d34580cb4e230a]

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
-/+ destroy and then create replacement

Terraform will perform the following actions:

  # aws_security_group.test_sg must be replaced
-/+ resource "aws_security_group" "test_sg" {
      ~ arn                    = "arn:aws:ec2:us-east-1:155569052071:security-group/sg-071d34580cb4e230a" -> (known after apply)
      ~ egress                 = [] -> (known after apply)
      ~ id                     = "sg-071d34580cb4e230a" -> (known after apply)
      ~ ingress                = [] -> (known after apply)
      ~ name                   = "test-sg" -> "test-sg-01" # forces replacement
      + name_prefix            = (known after apply)
      ~ owner_id               = "155569052071" -> (known after apply)
      - tags                   = {} -> null
      ~ tags_all               = {} -> (known after apply)
      ~ vpc_id                 = "vpc-0c67c57877889268e" -> (known after apply)
        # (3 unchanged attributes hidden)
    }

Plan: 1 to add, 0 to change, 1 to destroy.

─────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so Terraform can't
guarantee to take exactly these actions if you run "terraform apply" now.
